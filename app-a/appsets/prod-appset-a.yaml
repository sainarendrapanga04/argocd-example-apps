apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: prod-appset-a
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/sainarendrapanga04/argocd-example-apps
        revision: HEAD
        directories:
          - path: app-a/appsets/*

  template:
    metadata:
      name: prod-{{path.basename}}
    spec:
      project: app-a
      source:
        repoURL: https://github.com/sainarendrapanga04/argocd-example-apps
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: prod
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
